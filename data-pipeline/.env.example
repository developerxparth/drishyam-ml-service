# Same MongoDB your drishyam-backend uses (local or Atlas).
# This script writes directly into the climaterecords collection,
# so make sure this points at the SAME database the backend reads from.
MONGODB_URI=mongodb://127.0.0.1:27017/drishyam
