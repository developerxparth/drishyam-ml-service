# Same MongoDB your drishyam-backend uses. This service reads
# climaterecords directly — no backend changes needed.
MONGODB_URI=mongodb://127.0.0.1:27017/drishyam

# Port FastAPI listens on locally / in the container
PORT=8000
